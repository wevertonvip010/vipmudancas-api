const express = require('express'); const router = express.Router(); router.get('/', (req, res) => { res.status(200).json({ sucesso: true, dados: { message: 'Rota de contratoStorage funcionando' }, erro: null }); }); module.exports = router;
